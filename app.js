window.onload = function() {

  
  
  // let numero = document.getElementsByClassName("item").textContent;
  let display = document.getElementById("pDisplay");



}

function clickedFunc(numero) {
  if(pDisplay.length < 1) {
    pDisplay.textContent = numero;
  }
  else {
    console.log("full");
    
  }
  
}